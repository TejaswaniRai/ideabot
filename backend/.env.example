# ============================================
# Backend Environment Variables
# Copy this file to .env and fill in your actual values
# ============================================

# ============================================
# REQUIRED - API Configuration
# ============================================
# OpenRouter API key for AI features (Get from https://openrouter.ai/)
OPENROUTER_API_KEY=your-openrouter-api-key-here

# ============================================
# Server Configuration
# ============================================
# Port for the backend server
PORT=5000

# Node environment (development, production, test)
NODE_ENV=development

# ============================================
# Security & CORS
# ============================================
# CORS allowed origins (use * for development, specific domain for production)
# Example for production: https://your-frontend-domain.com
CORS_ORIGIN=*

# Rate limiting - max requests per 15 minutes per IP
RATE_LIMIT_MAX=100

# ============================================
# Logging
# ============================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# ============================================
# Production Deployment Instructions
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Replace 'your-openrouter-api-key-here' with your actual API key
# 3. NEVER commit the .env file to GitHub (it's in .gitignore)
# 
# For Railway/Render/Heroku deployment:
# - Add all required environment variables in platform settings
# - Set NODE_ENV=production
# - Set CORS_ORIGIN to your frontend domain (e.g., https://myapp.vercel.app)
# - Ensure OPENROUTER_API_KEY is configured
# - Adjust RATE_LIMIT_MAX based on expected traffic (default: 100)
#
# For Vercel/Netlify deployment:
# - Add OPENROUTER_API_KEY in dashboard under Settings > Environment Variables
# - Backend deployment on these platforms may require serverless functions

